signature BaseMonad ( M, listDirectory, loadFile ) where

import Value

data M :: Type -> Type

instance Functor M
instance Applicative M
instance Monad M

listDirectory :: FilePath -> M [FilePath]

loadFile :: (FilePath -> M Value) -> FilePath -> M Value
